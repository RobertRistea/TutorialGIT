#include <stdio.h>

int main()
{
    int a , b , s, dif;
    scanf("%d%d", &a, &b);
    s = a +b;
    dif = a - b;
    printf("Diferenta este: %d \n", dif);
    printf("Suma este: %d", s);

    return 0;
}
